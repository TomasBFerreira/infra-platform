# Source of truth for which (repo, env) pairs ARC manages.
#
# Mirror of ansible/github-runner/registered-repos.yml but per-repo, with
# explicit min/max replicas. The arc-deploy.yml workflow reads this file and
# helm-installs one scale set per matching entry.
#
# Phase 0: a single pilot scale set in dev only, label "dev-arc" so it doesn't
# collide with the existing LXC runner's "dev" label. Pilot repo runs the
# arc-smoketest.yml workflow against [self-hosted, dev-arc] to validate the
# stack before we cut any real workflow over.
#
# Cutover plan: as each repo migrates, change its scale-set label here from
# "<env>-arc" to "<env>" and decommission the LXC runner registration. See
# docs/arc-migration.md for the per-phase steps.

scale_sets:
  # ── infra-platform per-env ARC scale sets (network-vm split Phase 1) ────
  # Each env's traefik / adguard / cloudflared pipelines target
  # `runs-on: arc-pilot-<env>`. The dev entry was retired on 2026-05-26 as
  # a cleanup after the initial smoketest finished — but the cleanup was
  # premature: cloudflared_pipeline.yml dispatch for env=dev (2026-06-01)
  # sat queued indefinitely because no runner pool matched the runs-on
  # label. Restored 2026-06-01 with the same shape as qa + prod. Run
  # arc-deploy.yml with env={dev,qa,prod} to provision the controller +
  # scale set on each env's k3s.
  - repo: TomasBFerreira/infra-platform
    env: dev
    label: dev-arc
    name: arc-pilot-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/infra-platform
    env: qa
    label: qa-arc
    name: arc-pilot-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/infra-platform
    env: prod
    label: prod-arc
    name: arc-pilot-prod
    min_runners: 0
    max_runners: 2

  # ── First real cutover (2026-05-13) ──────────────────────────────────────
  # Only the deploy.yml's two self-hosted jobs in traefik-gitops are wired to
  # this scale set; the ~48 diagnostic workflows in that repo still target the
  # LXC runner via `runs-on: self-hosted`. We'll migrate those once deploy.yml
  # has a week of clean runs through ARC.
  - repo: TomasBFerreira/traefik-gitops
    env: dev
    label: dev          # unused under ARC v0.10's name-based routing; kept for future modes
    name: traefik-gitops-dev
    min_runners: 0
    max_runners: 3

  # ── Streambox cutover (re-attempted 2026-05-14, push branch only) ───────
  # Streambox is the user's most-active CD path (Tomaj Flix deploys land
  # frequently). The push-to-main deploy now routes through this scale set
  # via the same build-on-ubuntu-latest + crane-pull-tarball + k3s-import
  # pattern that landing-page uses (the original 2026-05-13 cutover was
  # reverted because streambox previously built with `docker build` on the
  # deploy runner — ARC pods have no Docker daemon; that's been fixed in
  # streambox#52 + #54). workflow_dispatch path stays on the LXC runner as
  # a fallback during soak and is what prod deploys use.
  - repo: TomasBFerreira/streambox
    env: dev
    label: dev
    name: streambox-dev
    min_runners: 0
    max_runners: 2

  # ── landing-page cutover (build-on-ubuntu-latest + deploy-on-arc) ───────
  # Uses the standard ops-portal-style pattern: build/push on GH-hosted, then
  # deploy step crane-pulls the image and `k3s ctr images import`s it onto
  # the worker (airgap-friendly). Needs crane + kustomize baked in the
  # runner image (see Dockerfile).
  - repo: TomasBFerreira/landing-page
    env: dev
    label: dev
    name: landing-page-dev
    min_runners: 0
    max_runners: 2

  # ── signup-page onboarding (ARC-first new SPA, 2026-05-13) ──────────────
  # Public signup page for friends/family tomajflix accounts. Same
  # build-on-ubuntu-latest + crane-pull + k3s-import pattern as landing-page.
  - repo: TomasBFerreira/signup-page
    env: dev
    label: dev
    name: signup-page-dev
    min_runners: 0
    max_runners: 2

  # ── nextcloud dev path (staging for LXC → k3s migration, 2026-05-19) ────
  # Prod nextcloud still serves cloud.databaes.net from CT 105 on betsy; this
  # scale set is for the dev path so we can validate the k3s manifests under
  # ARC before any data move. Prod stays on the [self-hosted, prod] LXC
  # runner until ARC is rolled out to the prod cluster. Migration scoping:
  # /app/issues/nextcloud-lxc-to-k3s-migration.md.
  - repo: TomasBFerreira/nextcloud
    env: dev
    label: dev
    name: nextcloud-dev
    min_runners: 0
    max_runners: 2

  # ── Batched easy-lane fleet cutover (2026-05-19) ────────────────────────
  # 11 repos that share the build-on-ubuntu-latest + deploy-on-self-hosted
  # shape (crane pull + ctr import or kubectl apply). Each one needs only a
  # one-line `runs-on` flip in its own deploy.yml — no per-repo Dockerfile
  # / workflow rewrite. Tracking sheet:
  # /app/issues/arc-migration-2026-05.md.
  # Excluded from this batch:
  #   - ops-portal-incidents (other Claude session is active on it)
  #   - ops-portal-pipelines (no deploy.yml; only diagnostic workflows)
  - repo: TomasBFerreira/ops-portal-identity
    env: dev
    label: dev
    name: ops-portal-identity-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-cmdb
    env: dev
    label: dev
    name: ops-portal-cmdb-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-audit
    env: dev
    label: dev
    name: ops-portal-audit-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-infrastructure
    env: dev
    label: dev
    name: ops-portal-infrastructure-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-deployments
    env: dev
    label: dev
    name: ops-portal-deployments-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-domain
    env: dev
    label: dev
    name: ops-portal-domain-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-shell
    env: dev
    label: dev
    name: ops-portal-shell-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/databaes-status-api
    env: dev
    label: dev
    name: databaes-status-api-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/wikijs
    env: dev
    label: dev
    name: wikijs-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/media-stack
    env: dev
    label: dev
    name: media-stack-dev
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/otel-monitoring
    env: dev
    label: dev
    name: otel-monitoring-dev
    min_runners: 0
    max_runners: 2

  # ── QA fleet (2026-05-27) ───────────────────────────────────────────────
  # Mirrors the dev entries above onto the QA k3s cluster. The ARC controller
  # + arc-pilot-qa were already deployed in Phase 0b of the network-vm-split
  # workstream (2026-05-19); these 16 entries are the per-repo listeners that
  # back `<repo>-qa` runs-on labels. min=0 so they cost nothing while idle
  # (QA promotions are infrequent vs. dev push traffic).
  #
  # Repos' deploy.yml still currently routes env=qa to [self-hosted, qa]
  # (LXC); per-repo PRs flip that conditional to "<repo>-qa" after these
  # listeners are confirmed online.
  - repo: TomasBFerreira/traefik-gitops
    env: qa
    label: qa
    name: traefik-gitops-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/streambox
    env: qa
    label: qa
    name: streambox-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/landing-page
    env: qa
    label: qa
    name: landing-page-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/signup-page
    env: qa
    label: qa
    name: signup-page-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/nextcloud
    env: qa
    label: qa
    name: nextcloud-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-identity
    env: qa
    label: qa
    name: ops-portal-identity-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-cmdb
    env: qa
    label: qa
    name: ops-portal-cmdb-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-audit
    env: qa
    label: qa
    name: ops-portal-audit-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-infrastructure
    env: qa
    label: qa
    name: ops-portal-infrastructure-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-deployments
    env: qa
    label: qa
    name: ops-portal-deployments-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-domain
    env: qa
    label: qa
    name: ops-portal-domain-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-shell
    env: qa
    label: qa
    name: ops-portal-shell-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/databaes-status-api
    env: qa
    label: qa
    name: databaes-status-api-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/wikijs
    env: qa
    label: qa
    name: wikijs-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/media-stack
    env: qa
    label: qa
    name: media-stack-qa
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/otel-monitoring
    env: qa
    label: qa
    name: otel-monitoring-qa
    min_runners: 0
    max_runners: 2

  # ── Prod fleet (2026-05-27) ─────────────────────────────────────────────
  # Mirrors the dev + qa entries onto the prod k3s cluster. ARC controller
  # + arc-pilot-prod were already deployed in Phase 0b of the network-vm-split
  # workstream (2026-05-19). Per-repo deploy.yml conditionals still currently
  # route env=prod to [self-hosted, prod] (LXC); per-repo PRs flip that to
  # "<repo>-prod" after these listeners are confirmed online.
  - repo: TomasBFerreira/traefik-gitops
    env: prod
    label: prod
    name: traefik-gitops-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/streambox
    env: prod
    label: prod
    name: streambox-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/landing-page
    env: prod
    label: prod
    name: landing-page-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/signup-page
    env: prod
    label: prod
    name: signup-page-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/nextcloud
    env: prod
    label: prod
    name: nextcloud-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-identity
    env: prod
    label: prod
    name: ops-portal-identity-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-cmdb
    env: prod
    label: prod
    name: ops-portal-cmdb-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-audit
    env: prod
    label: prod
    name: ops-portal-audit-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-infrastructure
    env: prod
    label: prod
    name: ops-portal-infrastructure-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-deployments
    env: prod
    label: prod
    name: ops-portal-deployments-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-domain
    env: prod
    label: prod
    name: ops-portal-domain-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/ops-portal-shell
    env: prod
    label: prod
    name: ops-portal-shell-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/databaes-status-api
    env: prod
    label: prod
    name: databaes-status-api-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/wikijs
    env: prod
    label: prod
    name: wikijs-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/media-stack
    env: prod
    label: prod
    name: media-stack-prod
    min_runners: 0
    max_runners: 2
  - repo: TomasBFerreira/otel-monitoring
    env: prod
    label: prod
    name: otel-monitoring-prod
    min_runners: 0
    max_runners: 2
