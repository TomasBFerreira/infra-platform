name: Test devops-portal connectivity

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, dev]
    steps:
      - name: Test devops-portal and Authentik
        env:
          VAULT_ADDR: ${{ secrets.VAULT_BOOTSTRAP_ADDR }}
          VAULT_TOKEN: ${{ secrets.VAULT_BOOTSTRAP_ROOT_TOKEN }}
        run: |
          echo "=== Direct backend health ==="
          curl -sf --max-time 5 http://192.168.20.11:30085/api/health && echo " OK" || echo " FAILED"

          echo "=== Authentik outpost (192.168.20.75:9300) ==="
          curl -sf --max-time 5 http://192.168.20.75:9300/outpost.goauthentik.io/ping \
            -o /dev/null -w "HTTP %{http_code}\n" || echo "UNREACHABLE"

          echo "=== Authentik outpost (192.168.20.76:9300) ==="
          curl -sf --max-time 5 http://192.168.20.76:9300/outpost.goauthentik.io/ping \
            -o /dev/null -w "HTTP %{http_code}\n" || echo "UNREACHABLE"

          echo "=== Authentik main app (192.168.20.75:9000) ==="
          curl -sf --max-time 5 http://192.168.20.75:9000/-/health/ready/ \
            -o /dev/null -w "HTTP %{http_code}\n" || echo "UNREACHABLE"

          echo "=== Authentik main app (192.168.20.76:9000) ==="
          curl -sf --max-time 5 http://192.168.20.76:9000/-/health/ready/ \
            -o /dev/null -w "HTTP %{http_code}\n" || echo "UNREACHABLE"
