name: Copy vault secrets to minecraft

# One-off provisioning workflow, same shape as
# copy-vault-token-media-stack.yml: brand-new repos don't inherit anything
# (TomasBFerreira is a personal account, not an org, so there's no
# org-level secret inheritance) — this seeds the 4 secrets
# minecraft/.github/workflows/deploy.yml needs from infra-platform's own.
# Safe to leave in place after use, matching the media-stack precedent.
on:
  workflow_dispatch:

jobs:
  copy:
    runs-on: [self-hosted, prod]
    steps:
      - name: Set vault secrets in minecraft
        run: |
          apt-get install -y -q python3-nacl
          python3 - <<PYEOF
          import os, base64, json, urllib.request
          from nacl import encoding, public

          gh_token = os.environ["GH_PAT"]
          repo     = "TomasBFerreira/minecraft"
          secrets  = {
              "VAULT_BOOTSTRAP_ADDR":       os.environ["VAULT_BOOTSTRAP_ADDR"],
              "VAULT_BOOTSTRAP_ROOT_TOKEN": os.environ["VAULT_BOOTSTRAP_ROOT_TOKEN"],
              "VAULT_PROD_ADDR":            os.environ["VAULT_PROD_ADDR"],
              "VAULT_PROD_TOKEN":           os.environ["VAULT_PROD_TOKEN"],
              # Added after the fact — import-world.yml needs GH_PAT to
              # `gh release download` from this same repo, and this got
              # missed on the first pass (only the 4 vault secrets were
              # copied, not the API token itself).
              "GH_PAT":                     gh_token,
          }

          req = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/actions/secrets/public-key",
              headers={"Authorization": f"Bearer {gh_token}", "Accept": "application/vnd.github+json"}
          )
          key_data = json.load(urllib.request.urlopen(req))
          key_id   = key_data["key_id"]
          pub_key  = public.PublicKey(key_data["key"].encode(), encoding.Base64Encoder)
          box      = public.SealedBox(pub_key)

          for name, value in secrets.items():
              encrypted = base64.b64encode(box.encrypt(value.encode())).decode()
              payload = json.dumps({"encrypted_value": encrypted, "key_id": key_id}).encode()
              req = urllib.request.Request(
                  f"https://api.github.com/repos/{repo}/actions/secrets/{name}",
                  data=payload,
                  method="PUT",
                  headers={
                      "Authorization": f"Bearer {gh_token}",
                      "Accept": "application/vnd.github+json",
                      "Content-Type": "application/json"
                  }
              )
              urllib.request.urlopen(req)
              print(f"Secret {name} updated in {repo}")
          PYEOF
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          VAULT_BOOTSTRAP_ADDR: ${{ secrets.VAULT_BOOTSTRAP_ADDR }}
          VAULT_BOOTSTRAP_ROOT_TOKEN: ${{ secrets.VAULT_BOOTSTRAP_ROOT_TOKEN }}
          VAULT_PROD_ADDR: ${{ secrets.VAULT_PROD_ADDR }}
          VAULT_PROD_TOKEN: ${{ secrets.VAULT_PROD_TOKEN }}
